/** @type {import('next').NextConfig} */

module.exports = {
    i18n: {
        locales: ['en', 'ru'],
        defaultLocale: 'en',
    },
    };
const nextConfig = {}

module.exports = nextConfig;

